# tpt-av TODO

## Project Setup
- [ ] Init Go module (`go mod init github.com/yourusername/tpt-av`)
- [ ] Create directory structure (`cmd/`, `internal/`, `web/`, `config/`, `systemd/`, `windows/`)
- [ ] Add `.gitignore`, `LICENSE`, `README` skeleton

## Shared / Internal
- [ ] `internal/config` — TOML config loader (shared structs for Guard + Patrol)
- [ ] `internal/events` — JSONL event log writer + reader (append-only, shared between daemons)
- [ ] `internal/db` — SQLite helper (open/migrate, baseline table, threat cache table, quarantine table)

## TPT Guard
- [ ] Guard config struct + TOML parsing
- [ ] Linux: nftables rule manager via `google/nftables` (default-deny, add/remove allow rules)
- [ ] Linux: DNS stub resolver (`miekg/dns`) with domain whitelist on `127.0.0.1:53`
- [ ] Windows: WFP user-mode integration (`golang.org/x/sys/windows`) — or `netsh advfirewall` fallback
- [ ] Guard REST API (localhost:7731) — status, rules CRUD, events tail
- [ ] Guard event log integration (`connection_blocked`, `connection_allowed`, `rule_added`, `rule_removed`)
- [ ] Guard systemd unit file (`/etc/systemd/system/tpt-guard.service`)
- [ ] Guard Windows Service wrapper (`golang.org/x/sys/windows/svc`)

## TPT Patrol
- [ ] Patrol config struct + TOML parsing
- [ ] Baseline builder — walk dirs, SHA-256 each file, store in SQLite `(path, hash, size, mtime, scanned_at)`
- [ ] Folder quick pre-check — compare `file_count + total_bytes` before hashing (skip if unchanged)
- [ ] Exclusion list filtering — skip configured paths and file extensions
- [ ] Real-time watcher — `fsnotify` on watched paths with 500ms debounce
- [ ] Scheduled scan loop — cron expression + `scan_window` time-of-day restriction
- [ ] Below-normal OS priority for scan goroutines (Linux `syscall.Setpriority`; Windows `SetThreadPriority`)
- [ ] Per-batch I/O delay — configurable `batch_delay_ms` sleep between file reads
- [ ] Process creation monitor — Linux: netlink connector (`PROC_EVENT_EXEC`); Windows: WMI `Win32_Process`
- [ ] Process baseline — hash running executables; flag new/changed exe on next process event
- [ ] Quarantine system — move + UUID-rename file, save metadata to SQLite
- [ ] Quarantine AES-256 encryption (optional, configurable)
- [ ] Quarantine CLI: `list`, `restore <id>`, `delete <id>`
- [ ] Threat feed: MalwareBazaar client (free hash lookup, no API key)
- [ ] Threat feed: VirusTotal client (optional API key, 4 req/min free tier)
- [ ] Threat feed local cache in SQLite — `(hash, verdict, source, checked_at)`, 7-day TTL
- [ ] Email alert system — SMTP, per-type cooldown (default 5 min), configurable severity threshold
- [ ] Patrol REST API (localhost:7732) — status, scan trigger, baseline, quarantine endpoints, events tail
- [ ] Patrol event log integration (`file_changed`, `new_file`, `file_deleted`, `threat_detected`, `process_created`, `process_anomaly`, `scan_complete`)
- [ ] Patrol systemd unit file (`/etc/systemd/system/tpt-patrol.service`)
- [ ] Patrol Windows Service wrapper

## CLI (tptctl)
- [ ] Cobra CLI skeleton + root command
- [ ] `guard status` — show Guard daemon state, rule count
- [ ] `guard rules list` — table of current whitelist rules
- [ ] `guard rules allow --process <name> --domain <pattern>` — add allow rule
- [ ] `guard rules deny --ip <cidr>` — add explicit deny rule
- [ ] `patrol status` — watcher state, last scan time, quarantine count
- [ ] `patrol scan [--path <dir>]` — trigger immediate scan
- [ ] `patrol baseline [--rebuild]` — show or rebuild baseline
- [ ] `patrol quarantine list` — show quarantined files
- [ ] `patrol quarantine restore <id>` — move file back to original path
- [ ] `patrol quarantine delete <id>` — permanently delete
- [ ] `patrol events [--tail] [--severity warn]` — stream Patrol events
- [ ] `events [--tail] [--source guard|patrol]` — stream shared event log

## Web Dashboard
- [ ] Single-page HTML + vanilla JS (fetch API, no framework)
- [ ] Guard panel: rules table, add/remove controls
- [ ] Patrol panel: recent events feed, last scan time, quarantine count
- [ ] Auto-refresh every 10s via fetch
- [ ] Embed static files via `embed.FS` in daemon binary
- [ ] Serve on port 7730 (mux both Guard and Patrol routes, or standalone)
- [ ] Warn in UI if dashboard is bound to `0.0.0.0` instead of localhost

## Cross-platform & Build
- [ ] `Makefile` — `build-linux`, `build-windows`, `build-all` targets
- [ ] Cross-compile targets: `linux/amd64`, `linux/arm64`, `windows/amd64`
- [ ] Test on Linux (Ubuntu/Debian)
- [ ] Test on Windows 11
- [ ] Test on headless VPS (Linux, no GUI)

## Documentation
- [ ] `README.md` — installation, config, quick-start usage
- [ ] `config/guard.toml.example` — annotated example Guard config
- [ ] `config/patrol.toml.example` — annotated example Patrol config
- [ ] Systemd setup instructions (enable, start, journal logs)
- [ ] Windows Service setup instructions (sc.exe, event viewer)

---

## Event Types Reference

| Source  | Type               | Severity |
|---------|--------------------|----------|
| patrol  | file_changed       | warn     |
| patrol  | new_file           | info     |
| patrol  | file_deleted       | warn     |
| patrol  | threat_detected    | critical |
| patrol  | process_created    | info     |
| patrol  | process_anomaly    | warn     |
| patrol  | scan_complete      | info     |
| guard   | connection_blocked | info     |
| guard   | connection_allowed | debug    |
| guard   | rule_added         | info     |
| guard   | rule_removed       | info     |

## Verification Checklist
- [ ] Rebuild baseline → modify watched file → event appears in `tptctl events --tail`
- [ ] Rebuild baseline → scan with no changes → verify instant skip (no hashes computed)
- [ ] Submit known-bad hash → `threat_detected` event + file moved to quarantine
- [ ] `tptctl patrol quarantine list` shows entry; `restore` moves file back
- [ ] Trigger threat → email received at configured address
- [ ] Start new process not in baseline → `process_created` event logged
- [ ] Outbound connection from non-whitelisted process → `connection_blocked` event
- [ ] Add process to whitelist → connection succeeds
- [ ] Run scan with `batch_delay_ms = 50` on large dir → CPU stays low
- [ ] `make build-linux` + `make build-windows` → both binaries produced
- [ ] Deploy to Ubuntu VPS → systemd service starts, events log populates
